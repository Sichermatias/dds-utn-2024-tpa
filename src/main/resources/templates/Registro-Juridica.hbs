<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Registro Persona Jurídica</title>
    <link rel="stylesheet" type="text/css" href="/css/StyleRegistroJuridica.css">
</head>
<body>
<header>
    <div class="navbar">
        <a href="/" class="logo">SISTEMA MAACVS</a>
        <nav>
            <a href="/inicio">Inicio</a>
            <a href="/mapa">Mapa</a>
            <a href="/nosotros">Acerca de Nosotros</a>
            <a href="/registro" class="active">Registrar</a>
        </nav>
    </div>
</header>

<main>
    <div class="form-container">
        <form action="/registro/juridica" method="POST">
            <h1>Registrar Persona Jurídica</h1>

            <!-- Fila: Razón Social y Tipo de Persona Jurídica -->
            <div class="row">
                <input type="text" id="razonSocial" name="razonSocial" placeholder="Razón Social" required>

                <input type="text" list="tipoPersonaJuridicaOptions" id="tipoPersonaJuridica" name="tipoPersonaJuridica" placeholder="Tipo de Persona Jurídica">
                <datalist id="tipoPersonaJuridicaOptions">
                    <option value="S.A">
                    <option value="S.R.L.">
                    <option value="S.A.S">
                    <option value="A.C">
                    <option value="Fundacion">
                </datalist>
            </div>

            <!-- Fila: Rubro y Dirección -->
            <div class="row">
                <input type="text" list="rubroOptions" id="rubro" name="rubro" placeholder="Rubro">
                <datalist id="rubroOptions">
                    <option value="Comercio">
                    <option value="Gastronomia">
                    <option value="Servicios">
                    <option value="Industria">
                </datalist>
                <input type="text" id="direccion" name="direccion" placeholder="Dirección (opcional)">
            </div>

            <div id="contact-container">
                <div class="row">
                    <input type="text" list="nombreContactoOptions" id="nombreContacto" name="nombreContacto[]" placeholder="Medio de Contacto">
                    <datalist id="nombreContactoOptions">
                        <option value="E-MAIL">
                        <option value="WHATSAPP">
                        <option value="TELEFONO LINEA">
                    </datalist>
                    <input type="text" id="contacto" name="contacto[]" placeholder="Dato de Contacto">
                    <button type="button" id="addContact" class="add-button">Agregar otro medio</button>
                </div>
            </div>

            <!-- Fila: Nombre de Usuario y Contraseña -->
            <div class="row">
                <input type="text" id="nombreUsuario" name="nombreUsuario" placeholder="Nombre de Usuario" required>
                <input type="password" id="password" name="password" placeholder="Contraseña" required>
            </div>

            <!-- Botón para crear Persona Jurídica -->
            <button type="submit">Crear Persona Jurídica</button>
        </form>
    </div>
</main>

<script>
    document.getElementById('addContact').addEventListener('click', function() {
        const contactContainer = document.getElementById('contact-container');
        const currentContacts = contactContainer.querySelectorAll('.row').length;

        if (currentContacts < 3) {  // Limitar a 3 medios de contacto
            const newContactRow = document.createElement('div');
            newContactRow.classList.add('row');

            newContactRow.innerHTML = `
                <input type="text" list="nombreContactoOptions" name="nombreContacto[]" placeholder="Medio de Contacto">
                <input type="text" name="contacto[]" placeholder="Dato de Contacto">
                <button type="button" class="remove-button">Eliminar</button>
            `;

            contactContainer.appendChild(newContactRow);

            // Añadir evento al botón de eliminar
            newContactRow.querySelector('.remove-button').addEventListener('click', function() {
                newContactRow.remove();
            });
        } else {
            alert("Solo puedes agregar hasta 3 medios de contacto.");
        }
    });
</script>
<style>
/* Ajustar altura de los botones para coincidir con los inputs */
.add-button, .remove-button {
    margin-left: 10px;
    padding: 10px; /* Coincide con el padding de los inputs */
    background-color: #b22222; /* Color rojo */
    color: white;
    border: none;
    border-radius: 4px;
    cursor: pointer;
    font-size: 14px;
    height: 40px; /* Ajusta la altura para coincidir con los inputs */
    width: 48%;
}

.remove-button {
    background-color: #8b1a1a; /* Color rojo oscuro */
}

.add-button:hover, .remove-button:hover {
    background-color: #a81818;
}

/* Ajustar los campos de entrada */
input[type="text"], input[type="password"], select {
    width: 48%;
    padding: 10px; /* Padding ajustado */
    margin-bottom: 15px;
    border-radius: 4px;
    border: 1px solid #ccc;
    font-size: 14px;
}
</style>

</body>
</html>
